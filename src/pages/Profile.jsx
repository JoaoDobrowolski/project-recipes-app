import React from 'react';
import Header from '../componentes/Header';

function Profile() {
  return (
    <div>
      <Header value={ Profile } search={ false } />
    </div>
  );
}

export default Profile;
